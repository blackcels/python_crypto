class output
from partie_cryptage.huffman import HuffmanCode

class output:
	def __init__(self, huff):
		self.token = "NONE"
		self.huff = huff

	def code_processing(output):
		if ("/share" in output)
			if (self.token == "HUFF"):
				huff.encode(output)
				return
		elif ("/msg" in output):
			if self.token == "NONE":
		if self.token == "NONE":
			print(input)
		elif self.token == "HUFF":
			print(self.huff.decode())

