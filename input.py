from partie_cryptage.huffman import HuffmanCode

class Input:
	def __init__(self, huff):
		self.token = "NONE"
		self.huff = huff

	def code_processing(input):
		if ("/share" in input)
		list = input.split(" ")
			self.token = list[1]
			list.pop(0)
			list.pop(0)
			if (self.token == "HUFF"):
				huff.accept_list(list)
				return
		if self.token == "NONE":
			print(input)
		elif self.token == "HUFF":
			print(self.huff.decode())

